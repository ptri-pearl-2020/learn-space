import React from "react";
// the Dashboard will trigger the load questions action and load the course's questions into state.questions

const Question = () => {
  const onSubmit = (e) => {};
  return (
    <></>
    // will sent a post request to the backend
    // compares the answers
    // gets a response from the backend
    // if right
    // then increase user score
  );
};

export default Question;
